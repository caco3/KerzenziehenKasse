<?

// Set this line to use the separate test system (separate DB, folders/files)
$testystem = true;

error_reporting(~E_ALL);
error_reporting(E_ALL); // Enable showing all error

// ini_set('display_errors', 1);
// ini_set('display_startup_errors', 1);

?>
